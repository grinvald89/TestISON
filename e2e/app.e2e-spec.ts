import { TestISONEPage } from './app.po';

describe('test-isone App', () => {
  let page: TestISONEPage;

  beforeEach(() => {
    page = new TestISONEPage();
  });

  // it('should display welcome message', () => {
  //   page.navigateTo();
  //   expect(page.getParagraphText()).toEqual('Welcome to app!!');
  // });
});
